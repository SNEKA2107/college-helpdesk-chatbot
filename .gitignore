node_modules/
backend/node_modules/
.env
backend/.env
*.log
dist/
screenshots/
test-screenshots/
test-login.js
open-login.js
*.png
!icons/*.png
!frontend/public/icons/*.png
# Android branding assets (launcher icons + splash screens) must be tracked
!frontend/android/app/src/main/res/**/*.png
.claude/
debug-*.png
live-site.png
login-*.png
test-screenshots/

# real auth tokens for test runners (template lives in automated_test/)
input.json

.vercel
.env*

# ── Student data exports (contain names, emails and phone numbers) ──────────
# Never commit these. The seeded equivalents are regenerated by
# backend/seed-students.js and backend/dev-local.js, so nothing here is needed
# to build or run the project.
students data.txt
students data copy.txt
students-export.csv
students-export.sql

# ── Local working copies of this repo (not part of the project) ─────────────
college-helpdesk-chatbot/
demo-clone/
viva-clone/
viva-clone2/
repository-name/

# ── Build / test artifacts ─────────────────────────────────────────────────
.pytest_cache/
device-screenshots/
screenshots-phase1/
releases/
*.apk
