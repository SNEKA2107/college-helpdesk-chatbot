{
  "name": "campusassist",
  "version": "1.0.0",
  "description": "CampusAssist College Helpdesk",
  "scripts": {
    "start": "node backend/server.js",
    "build": "node generate-icons.js && cd backend && npm install --omit=dev"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dependencies": "^0.0.1",
    "launch-website": "^7.0.0"
  }
}
